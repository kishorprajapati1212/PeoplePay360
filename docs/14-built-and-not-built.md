# 14 · What is built, and what is not

Written after round 4, because "is this actually there, or is it a picture of a screen?" deserved one
place with the answer. Everything below was read out of the code in this tree — the counts come from
`node backend/scripts/routes-list.js`, and the "no UI" list from scanning `frontend/src` for each
endpoint path (the script that produced it is described at the end).

Two rules were kept while writing it: if a button exists, the endpoint behind it exists and is
registered; if a sentence describes a role, it was checked against `permissionsFor()` rather than
remembered.

---

## 1 · Round 4 — what each complaint turned out to be

| You said | What was actually wrong | What is there now |
|---|---|---|
| "the payroll admin cannot enter the system, even when I create a new one" | `frontend/src/pages/LoginPage.jsx` offered a **Payroll Admin** button that filled `payroll.admin@oxp.com`, while the seeder creates `payroll-admin@oxp.com` (hyphen). Clicking it could only fail, and a hand-typed retry then hit the login guard (8 attempts / 5 min, then 15 min cooling off). A newly created login also had no password shown or asked for, so nobody knew what to type. | The demo list is generated from the same addresses the seeder writes, and lists each role's real powers. `hr2@oxp.com` is on it too (it was seeded but never printed or documented). Creating a user now has an optional **Temporary password** box, and the success toast says which of the two passwords works. The password box on the login card can be shown while typing. |
| "payslip generation shows compute but there is no result" | Two bugs stacked. The run detail page read `run0.payslips`, but `GET /api/payruns/:id` answers with **`employees`** — so the table was empty whatever you computed. And the Compute button threw its response away and always said "Computed every payslip". | The table reads the run's own rows (`payrun_employees` joined to slips): compute state, gross/deductions/net, days, slip status, PDF and email per person, with a **Show only the ones with problems** switch. Compute now prints what happened — `n of m computed · k flags · j blocking errors`, plus the failed names with their codes (`NO_CONTRACT`, …) — and Validate says why it refuses. |
| "add payrun does not give the right result" | The wizard promised *"Period ends — blank = end of the month"* while `payrun.service.js` did `to = period_end \|\| period_start`: a blank end date produced a **one-day** period, so pro-rata paid roughly 1/30th and half the candidates looked like joiners/leavers. | `resolvePeriodEnd()` is one function used by `preview()`, `create()` and the candidate list, so all three agree on the last day of the month. The wizard shows the date it will use, `Period ends` cannot be earlier than `Period starts` (red note + the API's own `DATE_RANGE` check), and Create takes you into the new run instead of dropping you back on the list. A duplicate period is refused with a message that names the run that already exists. |
| "the leave dropdown does not show correctly" (screenshot) | `GET /api/time-off/types` required `timeoff:approve` **or** `timeoff:type_write`. An employee has neither — only `timeoff:type_read` — so the request form got a 403, and `Select` drew its 34px box with nothing in it. Not a z-index problem: the popover is already a `z-[100]` portal. | The route accepts `timeoff:type_read` (same broadening for `/types/:id`, `/allocations`, `/balances/:employeeId`, `/overview`). `Select` grew the states it should always have had: *loading*, *failed to load* (red, with the message), and *nothing to choose yet* (amber, with a sentence about what to do). Arrow keys and Enter work, and scrolling the dialog repositions the list instead of closing it. `MyTimeOffPage` now explains the empty case and links to the screen that fixes it. |
| "it says HR will assign the balance but there is no button" | There was one place to grant a balance: *Time Off → Allocations → + New*, one person at a time. The types screen had nothing, and the employee note only said "ask HR". | **Assign balance** is a row button on Time-off types (per type), and Allocations has an **Assign balance to many** panel: pick the type, tick people (search, All in view, None), days, the validity window, and what to do when someone already has a grant (skip / add on top / replace). It posts to the new `POST /api/time-off/allocations/bulk`, which is one transaction and reports `{applied, created, adjusted, skipped[…], not_found}`. The "0 balance" note in *My time off* links straight to it with the type pre-selected. |
| "make sure the email is actually sent, and that it goes to many" | The only bulk send was *per run* (`POST /payruns/:id/send`), it appeared only when the run was already PAID, and its result was never shown. Fixing one bounced slip meant opening each payslip. | New `POST /api/payslips/send` — a bulk release for a **selection**: by payslip ids, by payrun, by period (`YYYY-MM`), by employee list, or any combination. It skips rows without a PDF or a work email instead of failing the batch, defaults to "only what has not been sent for this document version", and returns the counts plus how it will be delivered (SMTP/Gmail via the worker, or the preview driver writing `.eml` files when no credentials are configured — now said in the toast, so a demo send that "did nothing" is explained rather than guessed at). Buttons: *Payslips → ✉ Email payslips…* (period or run), a per-row **Email** for one slip, and the run's own *Email payslips* step which reports `queued / skipped` from the response. |
| "some fields show active but there is no deactivate" | `CrudPage` had Edit and Delete only. Turning a configuration row off was possible in the API (`is_active` on types, structures, departments) with no way to reach it, and Delete is refused for exactly those rows because history points at them. | `CrudPage` grew an `active` prop → a per-row **Deactivate / Activate** button that PATCHes just that flag (boolean for departments, `ACTIVE`/`INACTIVE` for types and structures, decided from the row's own value), with a confirmation that says what stays untouched. Leave types also got **Show inactive** (the list accepts `?include_inactive=true`) so a switched-off row can be found again. Salary rules have no flag — they are retired by `active_to`, so they got a **Retire** row button that writes the date for you. Working schedules and Users already had their own switches. |
| "the seeder's payroll-manager data is incorrect" | `payroll@oxp.com` was seeded as `HR_PAYROLL_USER + HR_MANAGER` while the README told you it *cannot* validate, mark paid or void a run — it can (the deny list is only a veto when no other role grants it). `payroll-admin@oxp.com` was `HR_PAYROLL_MANAGER` alone, which is the role that *can* bulk-mail. | The seeds now match the documentation instead of contradicting it: `payroll@oxp.com` = `HR_PAYROLL_MANAGER` (owns the run end to end), `payroll-admin@oxp.com` = `HR_PAYROLL_MANAGER + HR_MANAGER` (the same, plus fixing the employee/contract behind a wrong slip), `hr2@oxp.com` = `HR_PAYROLL_USER` (compute/validate/mark paid, no bulk mail). The README table, `ROLE_HINT` in the Users screen and the login-page buttons all say the same, checked against `permissionsFor()` — the matrix in this repo's README §"Roles" is generated from that file. |
| "a warning is shown but clicking shows no warning" | The chip counted payslips whose `computation_summary → warnings` array is non-empty; the detail panel called `GET /payruns/:id/warnings`, which read a **different** source (`payslip_lines.computation_log ILIKE 'ERROR%'`). A soft warning therefore produced "1 warn" beside "No warnings". | Both read `computation_summary → warnings`. The panel lists every entry with its severity (⛔ ERROR / ▲ WARN), the rule code that raised it, the employee, and a link to the slip. The list chip is now labelled `n flagged · m blocked` with a tooltip, because those are counts of *payslips affected*, not of individual checks. |
| "so many places have no placeholder or validation — account number etc." | Only the mobile field was cleaned. Bank account, IFSC, PAN, UAN, ESIC, PIN code, colours and codes were free text; a typo like `hdfc-1234` reached the API and came back as a generic field error. | `PATTERNS` in `frontend/src/components/crud/schemaForm.jsx` is the single table of formats: `bank_account` (6–18 digits), `ifsc` (`^[A-Z]{4}0[A-Z0-9]{6}$`), `pan`, `uan` (12), `esic` (17), `aadhaar`, `pincode`, `phone`, `hex_color`, `code`, `email`, `rupees`. Each declares a placeholder, an input transform (upper-case, digits-only, paste `+91 98250 12345` → `9825012345`) and the sentence shown under the box. `fieldProblems()` runs them on save, so the dialog turns red instead of sending; `CrudPage` and the employee dialog use it, Company settings uses the same table. Applied to: employee create + edit (bank, IFSC, PAN, UAN, ESIC, PIN, email, job title/placeholders), Users (name, email, temporary password ≥10), Company settings (PIN code, sender mailbox, plus the previously missing `country`, `currency`, `currency_symbol`, `pt_state`, `pt_charge_slice`), Leave types (code, payslip code, colour). |
| extra things found on the way | — | The **Payslips** register had a "Any payrun" filter hard-wired to `options={[]}` — an empty dropdown; it now loads the runs, and gained a *Bank details missing* filter. `LeaveTypesPage` offered a `HALF_DAYS` unit and an `approval_route: MANAGER_THEN_HR` that the API enum does not accept (a guaranteed 400); both are gone. The account menu's *My portal* was shown to staff roles that have no portal route; it is now derived from the role's own menu. `PATCH /api/payslips/:id/inputs` existed on the server and in `endpoints.js` with **no screen calling it**, so a rule that warned "no input" had nowhere to be fixed: the payslip page grew a **Manual inputs** panel (code, label, rupees, reason; remove; recompute after). `api/client.js` did not refresh on a 401 despite what the README said: it now rotates the refresh cookie once per burst (single-flight) and replays the failed call, so a 15-minute access TTL no longer empties a screen mid-edit. |

---

## 1b · Round 5 — the eight complaints, and what each one became

| the complaint | what the build does now | where it is enforced |
|---|---|---|
| A state must be chosen, not typed | A state list wherever a state is captured: the employee's home state, the company's registered-office state, and a separate Professional Tax state. The eight PT states carry a note ("Professional Tax applies here"), the slabs screen only loads for the state that has them, and a stored value that is not on the list is kept and labelled rather than cleared. | backend/src/lib/shared/india.js → GET /api/meta → frontend/src/utils/picklists.js (EmployeeFormModal.jsx, CompanyPage.jsx) |
| Dashboards per user type — "a new dashboard will give an error" | The screens come from a registry, not from conditions inside one component: payroll → the run view, HR → headcount, **employee → their own month** (hours, overtime, leave pending, balance per type, recent requests, last payslip). An unknown key renders a message that lists the registered kinds; it cannot crash and cannot silently render somebody else's screen. | frontend/src/pages/dashboards/registry.js, DashboardPage.jsx, OverviewDashboard.jsx, EmployeeDashboard.jsx |
| The link mail must go out by itself, one account at a time — not be parked in Redis | Creating an account (or *Send link*, or *Send links (N waiting)* for all of them) issues the single-use invitation and **sends the message from that request**: one SMTP conversation per account, in order, with the answer — sent, refused, or written to backend/storage/mail — reported per account. A task_queue row is still written first so the outcome is on Settings → System, and `INVITE_VIA_QUEUE=true` is the opt-in for one BullMQ job per account with the worker's retries instead. The link is also shown in the dialog with a Copy button, because with no SMTP credentials the mail exists only as an .eml. /set-password?token= is the screen; it is reachable while signed out. | backend/src/services/user.service.js (createInvite), queue job account-invite in src/worker/jobs/email.job.js, frontend/src/pages/auth/SetPasswordPage.jsx |
| One role per person; an admin must not change another admin | Exactly one of the five roles: POST /users/:id/role, and /roles accepted only as a one-item list (the validator refuses a longer one with the rule in the message). Changing a role revokes that person's sessions. An ADMIN cannot change a peer ADMIN's role, password or status; name and email stay editable, and the Users page shows "deactivate: self only" / "pw: self only" instead of a button that can only fail. | assertNotAnotherAdmin + setRole in backend/src/services/user.service.js, oneRole in src/validators/user.schema.js, frontend/src/pages/settings/UsersPage.jsx |
| Every "cannot delete" must say why, and offer deactivate | Postgres's own detail is translated into a sentence naming the table that holds the row ("…still used by employee rows… Deactivate it instead") and the delete dialog stays open with that reason inside it plus a Deactivate-instead button. A leave type that is in use gets its own counts (requests, allocations, slips) and can_deactivate. | friendlyReference in backend/src/lib/shared/errors.js, timeoff.service.js for the counted case, frontend/src/components/crud/CrudPage.jsx |
| The dropdown caret renders as an artefact | The picker's triangle is drawn inline at a fixed size and rotated on open, so it cannot inherit the text font's idea of ▼/▲ and cannot shift the label by a pixel between states. | frontend/src/components/ui/controls.jsx (Select) |
| Leave types need categories and a payoff category | time_off_types.category ∈ CASUAL SICK EARNED PRIVILEGED COMPENSATORY MATERNITY PATERNITY UNPAID LOP OTHER (migration 012, CHECK + index), shown as a column, filterable with ?category= and ?pay=. Pay treatment PAID \| UNPAID is the one control that writes is_unpaid (+ the LOP component); the redundant checkbox is gone. | backend/db/migrations/012_leave_category.sql, LEAVE_CATEGORIES in src/validators/hr.schema.js, frontend/src/pages/timeoff/LeaveTypesPage.jsx |
| Stale filters and blank states are bad UX | A list filtered to nothing says "Nothing matches these filters" and offers *Clear filters (n)* in the toolbar; a deep link whose row is gone (?assign=…) names the id and has a button that drops it; every Select that cannot load says "Choices failed to load" with the reason in its title, and one with nothing to pick says "Nothing to choose yet" instead of an empty box. | CrudPage.jsx (activeFilterCount, the empty state), AllocationsPage.jsx, controls.jsx |

**What round 5 did not do**: no e-mail verification or bounce handling for invitations (a refused send is reported and
the link stays usable; nothing retries it on its own unless the queue mode is switched on); no
self-service sign-up — an outside person cannot request a link, an admin has to create the account first; the
invitation is not revocable by hand (it expires, or is consumed by use); and the employee dashboard reads the portal summary
endpoint, which is month-scoped — there is no year view yet.

## 1c · Rounds 6 and 7 — who gets mailed, and what the screens said about themselves

Round 6 was about invitations (one mail per account, sent when it is asked for, never parked in a queue by
default; a waiting list you can send in bulk; a peer admin refused; a role change that really ends the old
sessions). Round 7 was nine things that could be seen from a browser. Each row is the complaint, what was
actually wrong once it was read out of the code, and where the fix lives.

| the complaint | what was actually wrong | what is there now |
|---|---|---|
| "why did you add the extra user, there is no need of it" | `USERS` carried a sixth row — a second ADMIN whose only job was to demonstrate the peer-admin rule the API already enforces. It appeared in the seeder's own printout and in the sign-in card. | Four staff logins (`admin@oxp.com`, `hr@oxp.com`, `hr2@oxp.com`, `payroll@oxp.com`), one per role, plus the employee accounts that `seedEmployees()` creates. The rule it demonstrated is unchanged: an admin editing a peer admin is refused with the reason, and the test for it is in `backend/test` — it does not need a spare account. |
| "when leave is raised or approved, nothing says if it is paid" | The leave type *knew* this (`category` + `pay_treatment` → `is_unpaid`), and the employee dialog half-said it as "(unpaid)" in a dropdown label. The approver's dialog said nothing at all. | `frontend/src/utils/leave.js` is the one sentence, written from the type: *"Paid from the casual balance — no payslip line, the days come off what is granted."* / *"Unpaid — these days go to Loss of Pay on the next payslip."* The employee sees it under the type picker before Apply; the approver sees it inside the Approve/Refuse box, with the number of days that will be recorded. `GET /time-off/requests` now carries `category` and `requires_allocation` so the sentence is read from the type, never guessed in the component. |
| "carry forward only offers Privilege" | The panel's type list was filtered by `carry_forward` itself, so a type whose policy said *no* never appeared — the feature looked broken for everything except PL. | Every type that grants a balance is offered. One whose policy blocks carrying is labelled *— does not carry forward yet*, and choosing it explains the refusal and offers **Allow carry-forward for <type>**, which PATCHes the type through the real endpoint and re-reads the list. The decision stays with the admin; the list does not hide it. |
| "attendance has an OT column with no meaning" | Two columns were both headed `OT`: `overtime_hours` and `overtime_approved`. Nothing in the header said which was which, and neither said why payroll cares. | They are headed **Overtime (h)** (hours the punches add up to, `—` when there are none) and **Overtime approval** (the chip plus the Approve button). Both are kept, deliberately: payroll pays overtime only once it is approved, so hiding either one would hide a step in that rule. The header carries a `title` that says the same in full. |
| "the department table has a Parent column nobody uses" | The list selected `parent_name` and printed a column, while nothing in the demo data has a parent — a column that was always `—`. | The column is gone. The field stays in the form (with a hint that it is for an org that really has a hierarchy), because the API accepts `parent_id` and deleting the input would remove a capability rather than decoration. |
| "assign leave balances in the seeder" | The seeder *did* grant balances, but from a hard-coded `[['CL',12],['PL',15],['SL',7]]`, so a type created on screen stayed ungranted and looked like a seeding bug. | The grant list is derived from `LEAVE_TYPES` (`requires_allocation` && `max_days_per_year`), and the seed log prints what it granted, so the list cannot go stale when a type is added or capped. |
| "each login type should start on one default route" | `landingFor()` returned the first entry of the role's menu — a layout decision, so editing a menu moved somebody's front door. Worse, it named `/my-pay`, which is not a route at all. | A table in `frontend/src/App.jsx` (`ADMIN` → `/dashboard`, `HR_MANAGER` → `/employees`, both payroll roles → `/payroll`, `EMPLOYEE` → `/portal`), each route checked against that role's own menu before it is used, a `/no-access` page for an account with nothing to open, and `scripts/ui-probe` asserting all five. |
| "work schedule has no default timing" | Not a missing default — a broken wire. The form posted day **names** (`mon`) where `POST /org/working-schedules` wants ISO numbers **1–7** (`z.coerce.number().min(1).max(7)`), so a save was refused; reading a row back did `String(1).slice(0,3)` → `'1'`, which matches no day, so an open dialog showed an empty week and the list's seven little boxes were all grey. | One mapping, two small functions (`dayNumberOf` / `dayKeyOf`), used by the save, the read and the week strip. `blank()` supplies the standard week (Mon–Fri 09:30–18:30, 60-minute break, Sat/Sun off) so a new schedule is never an empty grid; a per-row **Hours** column shows what each day adds up to before you save; and *Fill the standard 5-day week* re-derives it inside an existing schedule. The probe posts a save and asserts `day: 1…7`. |
| "the UI is too inconsistent to fix by hand; login is out of step; containers overflow" | Individual screens each carried their own width, padding and truncation choices: `SearchInput` was `w-56` here, `w-64` there, and the sign-in card had no theme switch at all. | Fixed where they are generated, not page by page: `SearchInput` sizes itself (`w-full min-w-0 sm:w-56`) and the seven fixed widths were deleted from the pages; `ThemeToggle` now appears on both signed-out screens (login card, and the absolute corner of the invite-link screen); the schedule grid scrolls rather than squeezing its `input[type=time]` boxes; long names in the schedule list truncate inside `min-w-0` instead of pushing the table. `DataTable` already wraps wide tables in `overflow-x-auto` and `Modal` scrolls its own body — both re-checked, not assumed. |

**What round 7 did not do**: it did not rewrite the styling system (there is still one `index.css` with utility
classes, not a token layer); it did not touch the light theme's palette beyond what `check-contrast.py` demanded;
the overtime columns are renamed and explained, not removed, because the pay rule needs both; and the carry-forward
switch changes a type's policy — it does not approve the run, which still re-checks the policy on the server.

## 1e · Round 9 — a schedule that could not save, an empty payrun, buttons hidden by a wrong word, minutes on a link

The long version, with the file and line behind each claim, is [`17-round-9-plan.md`](17-round-9-plan.md).

| Reported | What the code actually said | Now |
|---|---|---|
| "the working schedule updating show this error … also not done crud on it" | Two separate faults in one payload: a weekly off sent `start:''`, and `timeStr.optional()` means *absent*, not empty, so a day nobody works failed a clock-format check; while `dow = z.coerce.number()` called a day name `NaN` and reported "Expected number, received nan". And `DELETE /org/working-schedules/:id` existed, `org.schedules.remove` existed in the client, and **no button called either**. | `weekGridDay` in `validators/common.js` is one readable preprocessing step shared by both schedule schemas: `mon`/`SUN`/`monday` → 1…7, an empty box → absent, a rest day keeps no times. The page sends `{ day, rest: true }` for an off day, marks a worked day's two boxes as needed through `isTime` in `utils/form.js`, and says once above the grid which days are incomplete. The row gained **Deactivate/Activate** — a one-field `PATCH /working-schedules/:id/active`, because a button that means "switch this off" must not rebuild seven days from a list response — and **Delete**, which quotes the API's `SCHEDULE_IN_USE` counts back at you and offers the deactivation as the way out. |
| "the payrun … show the 4 assign but when i am doing it show none is aligiable … create 100 to 200 seeder data … only those physically possible and sesiable" | `EMPLOYEES` in the seeder was 13 rows, and every date in `RUNS`, `ATTENDANCE_MONTHS` and the financial-year window was written into the file as a 2026 literal — so the month a person actually lives in had nobody on the payroll, and the wizard answered with three guesses about why. | The company is grown to `HEADCOUNT` (160 by default, `SEED_EMPLOYEES`, capped at 400 so a laptop seed stays a minute): names, cities and wages from tables that fit the title; a DOB that makes every adult 22-58 today and never younger than 18 on their joining day; one contract each, covering the period, ending at the exit date for the few who left (employee `TERMINATED`, contract `EXPIRED` — a combination that was impossible before, since every row was forced ACTIVE); PAN / IFSC / UAN / ESIC / bank account / pincode / phone in the exact shapes `schemaForm.PATTERNS` accepts, so the edit dialog opens on a seeded row; ESIC only under the ₹21,000 limit; managers that point backwards so they cannot cycle; and only the two states `PT_SLABS` covers. Runs and attendance are counted back from **today**; the last `SEED_PAYRUN_MONTHS` (2) months are computed, validated and paid through the real engine, and this month stays a DRAFT run with everybody in it so the first button you press has work to do. An empty wizard now prints the counted reason — `why_empty` from `GET /api/payruns/employees`, over `eligibilityBreakdown` in `employee.repo.js`: not active / DRAFT contract / dates do not cover / already in a run. |
| "there is still leave or payoff approve button not show in any user" | `request_status` is `DRAFT | TO_APPROVE | APPROVED | REFUSED | CANCELLED` — there is no `PENDING`. The Approve/Refuse pair, the status filter, the employee's Cancel button and the overview's waiting list each compared a row to `'PENDING'`, so they were unreachable for every role, with the permission *grants* correct (`timeoff:approve` is in `HR_CORE` — though the payroll logins had the grant and no menu link, see the next row). The seeder also had 8 requests between 13 people. | `frontend/src/utils/leaveStatus.js` owns the words — `REQUEST_STATUS`, `isWaiting`, `isCancellable`, `STATUS_LABEL`, `STATUS_OPTIONS` — and the four screens use it; `StatusChip` has a tone for `TO_APPROVE` and the label reads *Waiting for approval*. The seeder builds the queue from the crowd: about one person in five has something waiting, dated **ahead** of today (an approver who finds a request that started three weeks ago reads the queue as broken), with approved history and refused rows carrying their reason. Overtime approval was already keyed on real columns and ~15% of seeded OT rows stay unapproved, so that button has work too. |
| "give me place where i can put [gmail + app password] and also want to know the email is going or not and password is working or not too like it should ttl like only 10 minute … if they update they can not do reset it" | **`POST /users/:id/invite` threw.** The success sentence interpolated a bare `driver` while the value in scope is `mailDriver`: the mail was sent, then the request died building its answer — which is what "the link mail never really sends" looked like from the outside, and no build or test saw it, because esbuild leaves an unresolved name in a template alone. The link also lived 72 *hours* with no way to say otherwise from the product. Single-use was already right (`accepted_at`, claimed atomically in `acceptInvitation`). | `mailDriver` everywhere, and `test-unit.js` now greps `createInvite` for a bare `driver`, which is the check that would have caught it. The window is minutes and it is a setting: `company_settings.mail_invite_ttl_minutes` (migration `015_invite_ttl_minutes.sql`, default 10, `INVITE_TTL_MINUTES` as the deployment fallback, clamped 1-1440), read through `inviteTtlMinutes()` in `mail.service.js`, and spoken about by one function — `linkTtlSentence` in `lib/mailer/template.js` — so the mail, the settings panel and the Users page cannot disagree. The invite result names the recipient and the driver and ticks the window down; when it closes, it says *it either expired or the password has been set*. *Check connection* separates the two faults: a refused login gets the App Password explanation, an unanswered host gets the port/TLS one. The panel is on Settings → Company, which needs `settings:write` — Admin only — and the page now says so in one line ("Read-only for your role") and hides the two mail buttons instead of letting them answer 403, so nobody hunts for a field that is deliberately not theirs. |

| …and the same complaint again, this time for the *payroll* logins | Approve/Refuse was reachable as an HR Manager but nobody else saw it: `ROLE_NAV.HR_PAYROLL_USER` and `HR_PAYROLL_MANAGER` are `'+payroll_user'` / `'+payrun_admin'`, i.e. Payroll and Settings only. Both roles held `timeoff:approve`, both could open `/api/time-off/requests` — no link, no screen, "the button is not show in any user". The file's own comment said "deny wins over allow", but `isDenied` only vetoes a permission no held role grants, so the payroll `DENIES` entries were text, not behaviour. | `TIMEOFF_GROUP` is declared once and put on the HR menu and the payroll overrides, so a granted power ships with its link. The payroll allow list is `HR_CORE_PAYROLL = HR_CORE.filter(p => !PAYROLL_NO_HR_ADMIN.includes(p))`, and the header comment says what a DENIES entry can and cannot do. `scripts/test-unit.js` asserts the invariant both ways for every role. |

## 1d · Round 8 — mail transport, required-field honesty, payslip files, structures that explain themselves

The plan carrying each complaint and the state the code was really in is
[`16-round-8-plan.md`](16-round-8-plan.md); this is what came of it.

| Reported | What was true in the code | Now |
|---|---|---|
| "the invite-link mail never really sends … i have the email password and email so make according to it" | `backend/.env` here is **empty**, so the driver was `preview`: every mail became a `.eml` file and nothing was sent. Changing that meant editing the file and restarting, which is not "having an email and a password in the product". | **Settings → Company → E-mail delivery** (migration `013_mail_transport.sql`): host / port / implicit TLS / account / App Password / from / daily cap / *Send real mail*, stored on `company_settings` and read by the API **and** the worker through `lib/mailer/runtime.js` (15 s cache, `invalidateMail()` on save). `GET /api/company/mail` reports the live driver without opening a connection; `POST /api/company/mail/check {to?}` is the only thing that connects — no address verifies the login, an address sends a real message. The password never comes back out (`withoutSecret` in `company.repo.js`, `smtp_password_set` for the UI), a blank save keeps what is stored, and `.env` stays the fallback. README §5d is the how-to. |
| "some field say `*` and some does not … even when click on submit it will show error" | Stars came from a hand-written `NEEDED` list (Contracts) or were absent (the leave dialog, the schedules dialog, the approval note). `components/crud/schemaForm.jsx` short-circuited on a blank value **before** running a field's `validate`, so "an amount, unless this is a percentage rule" could not be expressed at all. | `frontend/src/utils/form.js` (`isBlank` / `guard` / `missingSentence`) is the one checklist, derived from the same list the stars come from: Contracts, Working schedules (both dialogs), My leave, HR leave (raise and decision) and `fieldProblems` for every CRUD form. `validate` now runs before the blank short-circuit, and *Optional* replaces the star where a field may stay empty. The cross-check for rules also runs on the server (`RULE_INPUT_MISSING` in `salary.service.js#saveRule`), so an old row with a blank deciding value cannot be re-saved unchanged. |
| "payslip generation it is not working so make ui or functionality that simple" | Three separate truths: a single slip's download **renders on demand** (it works), the ZIP works, but `POST /payruns/:id/generate-pdfs` only wrote a `task_queue` row and answered "Queued" — and with no Redis that queue is a black hole. The button was hidden unless the run was `VALIDATED`/`PAID`, while the payslips register told people to "generate the PDFs on the payrun first", which the register itself could not do. | `generatePdfs(id, {force, mode})` queues, and when the queue throws (or nothing is listening) renders up to `PDF_INLINE_LIMIT` (40) slips inside the request, reporting per person: `{queued, inline, generated, failed[], waiting, already, queue_error, how}`; `mode:'queue'` keeps mark-paid's fail-loudly behaviour. A **Payslip PDFs** panel on the run says `1 of 3 payslips have a file ready`, draws the bar, offers *Create the 2 missing PDFs* and *Re-render all*, polls every 1.5 s until every slip has `pdf_generated_at` (with *stop watching*), and prints the outcome from the API's own numbers. The step appears at `COMPUTED` too, saying what it does, and the register gained *Create the missing PDFs* for the run in its filter. |
| "salary structure table is not good enough … not anything say specifically what it is" | Four columns, none of which said what a structure *pays*. And, found while wiring this: **the row click was never wired at all** — the structure dialog could not be opened from the list, because `CrudPage` had no row handler and the page's own `setOpen` was unreachable. | `CrudPage` gained `onRow`, forwarded to `DataTable`'s existing `onRowClick` and guarded so a click on a button, link, input or option opens nothing. The list explains itself: *Lines*, *Assigned to* (contracts per employee, a count whose tooltip names them), *Used in runs*, *Wage on those contracts* (*varies between*, when the contracts disagree). The dialog prints one sentence per rule from `utils/salary.js#explainRule` — the same sentence the Rules page now imports — and *Show the amounts* asks the real engine (`POST /api/salary/preview`) for an editable wage and days worked, showing each rule's `computation_log` plus Gross / Deductions / Net. The `explainRule` copy that was local to `RulesPage` is gone, and so is `RulesTable` — its rules sub-table, which the dialog now replaces; neither has a reference left anywhere. |
| "and did we fix the leave approval or not" | Partly: `remark` was finally sent, which was the missing payload key. Not built: any UI for approving fewer days than were asked for, and `timeOffRequestBody` requires a reason on a refusal — so the box could look harmless and still answer 400. | The decision box is titled for the action, labels its note *Reason for refusing* when refusing and will not submit without one, and offers *Approve fewer days than were asked for* with a number that errors when it exceeds the request. The employee sees the same text under *Note from HR*. Migration `014_leave_decision_remark.sql` keeps `refuse_reason` and `decision_remark` in step for anything reading the older column. |
| What the new tests caught in the shared parts | `guard()` had its blank test inverted (`isBlank` without the `!`) so it complained about *filled* fields and waved through empty ones, and a spec entry written as `[key, label]` counted as "not required" — together they made every hand-written guard in the app a no-op. `npm run build` passed with all of that broken. | `guard` treats naming a field as "needed" (`required = true`) and complains only about blank ones, line by line with comments. Two probe cases hold it in place: a refusal with no reason never reaches the API, and a schedule with no name is not saved. |

## 1f · Round 10 — mail from two boxes, and what Docker reads

| Reported | What the code actually said | Now |
|---|---|---|
| "use nodemailer which take only the email password and email name … i does not want to add so much env file" | `resolveDriver` already treated an address as a complete configuration — for exactly one provider, matched by a `/gmail\.com$/` regex — and anything else with no `SMTP_HOST` fell back to the preview driver. The settings screen then led with *host*, *port* and *implicit TLS*, so four boxes were in front of you where two worked, and `backend/.env` shipped `SMTP_PORT=587` uncommented, which out-ranks the address for a Gmail box. | `providers.js` is a table of seven providers (host, port, TLS, whether an app password is required, and the sentence that says so), and `resolveDriver` fills only what you left blank: Gmail, Outlook.com, Zoho, Yahoo, iCloud, Fastmail and QQ need an address and a password, nothing else. A host you typed still wins box for box, so a company relay is one field away, and an address that is not on the list is named in the answer rather than mailing nothing. `MailServerHint` says which server the typed address implies *before* saving, from the `providers` list the API returns — the frontend copies no rules. The two committed env lines are now the only uncommented mail keys in `backend/.env`. |
| "if i start as docker then which variable i have to change" | True answer, unwritten: `docker-compose.yml` mounts `backend/.env` read-only, and `config.js` fills a key only when `process.env` lacks it — so the keys compose itself sets (`DATABASE_URL`, `REDIS_HOST`, `WEB_ORIGIN`, `CORS_ORIGINS`, `JWT_SECRET`, `PORT`, `HOST`, `WORKER_PORT`, `NODE_ENV`) cannot be changed from `.env` at all, while everything else can. | README §1 has the table — mail, `PUBLIC_APP_URL`, demo size, invite TTL in `backend/.env` plus `docker compose restart api worker`; the compose-set keys in `docker-compose.yml`; and one row saying that mail needs **no file**: Settings → Company, live within a save. The compose header carries the short version. |
| "still shows the error … `getaddrinfo ENOTFOUND smtp.reply.example` … i use node mailer with simply give it email_name and email_password and it work" | Not a password problem, and the error said so: `ENOTFOUND` is DNS. A host box on the settings screen held a placeholder-looking value, and the rule I had just written — *what you typed wins* — made the app dial it instead of Gmail, for every address tried. The box had been encouraged: its placeholder was `smtp.relay.example`, and `backend/.env` carried the same shape in a comment. | `isPlaceholderHost()` in `providers.js` recognises reserved hosts (`.example`, `.test`, `.invalid`, `your-domain`, `changeme`, …) and nothing else, so `smtp.acme.com` or `localhost:1025` is still obeyed. A placeholder host is dropped — with the port and TLS typed next to it, which is why a Gmail row used to read `smtp.gmail.com:587 · implicit TLS` — and the plan's note names the box. `GET /company/mail` returns `conflict`; the panel shows it and offers **Clear the host box and use smtp.gmail.com:465 · implicit TLS** as one `PATCH /company` that empties host, port and TLS together. The field's placeholder is now a sentence, `.env` warns against the example value, and both directions are tested. |

## 1g · Round 11 - the invite that 500'd, and what a running database caught

| Reported | What the code actually said | Now |
|---|---|---|
| "invitation email with the reset link ... create it with proper" | Two separate failures, neither of them the mail server. `createInvite` read the link's lifetime before computing it (and imported the module that provides it below the use), so the route answered 500 for every account; and the invitation body existed twice, one copy in the API and one in the worker. | One `inviteMail` template, called by both paths and guarded by a test that refuses a second copy; the link, the row and the response are built in dependency order, `send_email:false` returns the link as the whole delivery, and the message is `multipart/alternative` so the raw URL sits on its own line in the text part. Run end to end over HTTP against Postgres: invite → link → `valid: true` → set password → the same link refused as already used → the employee signs in. |
| "make seeder which has not payrun yet ... need the 100 - 200 row" | The seeder always paid somebody: `SEED_PAYRUN_MONTHS=0` still wrote a paid month's worth of paperwork in places, and the crowd was a constant. | `--no-payruns` (alias `--skip-payroll`, `SEED_PAYRUNS=false`) skips payroll with a line in the banner, `SEED_EMPLOYEES` carries the crowd to 400, and the wrinkle pass no longer claims overtime rows it did not write. Measured: 160 employees, 9519 attendance rows, 34 rules, **0 pay runs, 0 PAID payslips**, 31 leave requests waiting. |
| "it does not work" (the Gmail 535) | Not the transport: Gmail itself answered the sandbox. The stored "App Password" is 14 characters and Google issues 16 in four groups of four, so every retry was doomed. | `appPasswordShapeHint` states the length rule on *Check connection* and under the box in Settings → Company; `GET /company/mail` returns `password_length` (never the value). `EMAIL_USER`/`EMAIL_PASS` are accepted beside `EMAIL_NAME`/`EMAIL_PASSWORD`, and the provider table now owns host, port and TLS as one triple when no host was typed, which retires the `smtp.gmail.com:587 · implicit TLS` line the column default produced. |
| "some where show round shape even in pc" | Ten `rounded-full` hits in the frontend, of which four were not circles at all: `StatusBreakdown`'s rails and the compute-progress bar on a pay run, each 6-8px tall with a full radius, so the ends drew semicircles on a wide window. | Those four are `rounded-sm` with the fill squared and the track clipping; the six that remain are a spinner, two avatar chips, a step badge and two legend dots, which are meant to be round. A probe case renders the breakdown and asserts the classes, and `run.mjs` now also greps the source of both files under `SOURCE_RULES`. |
| nothing reported it | `update task_queue set status = $2` beside `case when $2 = 'COMPLETED'` makes Postgres deduce two types for one parameter, so `markTask` failed for every task type in the system - and only a live database shows that, because the code path returns before the statement in a dry run. | `status = $2::queue_status` with `$2::text` on the comparisons, the shape `payslip.repo` already used; all five statuses driven against Postgres. The unit runner was fixed in the same breath: it never awaited a promise-returning test, so such a test counted as passed while the summary lied. |

## 2 · Built and wired (screen → endpoint → table)

Every row here has a real button or form on the named screen; nothing in this list is a stub.

| Area | Screens | Endpoints used |
|---|---|---|
| Sign-in, session, roles | Login card with role buttons, 401 refresh + replay, `/api/auth/me`-driven menu, permission-gated buttons everywhere | `POST /auth/login`, `/auth/logout`, `/auth/refresh`, `GET /auth/me`, `GET /auth/access-matrix` |
| Own password | Change-password modal in the account menu + the "still on the handed-over password" banner | `POST /auth/change-password` |
| Dashboard | Per-role tiles, counts, quick links | `GET /dashboard` |
| Employees | List, create (3 tabs: personal / job / salary+contract+bank), detail with contracts, attendance, time off, payslips, salary tabs, terminate dialog, manual inputs | `GET/POST /employees`, `GET/PATCH /employees/:id`, `POST /employees/:id/terminate` |
| Org | Departments (tree fields, activate/deactivate), working schedules (weekly grid, active switch), holidays (+ bulk generate) | `/org/departments*`, `/org/working-schedules*`, `/org/holidays*`, `/org/holiday-templates` |
| Contracts | List, create/edit, terminate, renew, expiring strip | `/contracts*`, `/contracts/:id/terminate`, `/contracts/:id/renew` |
| Attendance | Day grid, entry edit, clock in/out, overtime approval, exceptions, CSV export | `/attendance*`, `/attendance/clock`, `/attendance/:id/overtime`, `/attendance/exceptions`, `/attendance/export.csv`, `/payruns/:id/export.csv` |
| Time off | Types (category + pay treatment per type, policy fields, deactivate, assign balance), requests (approve/refuse/cancel, filterable by category and payoff), allocations (+ bulk grant), overview, carry-forward, my requests | `/time-off/*` incl. the new `/time-off/allocations/bulk` and `/time-off/carry-forward` |
| Salary structures & rules | Structures CRUD (+ deactivate, impact), rules CRUD with formula validator, preview against a wage, retire-by-date | `/salary/structures*`, `/salary/rules*`, `/salary/rules/validate`, `/salary/preview`, `/salary/pt-slabs` |
| Pay runs | Two-step wizard (structure, period, frequency, mode → candidates with estimated net), status board, compute/validate/PDFs/mark paid/email/void, salary-register CSV, PDF ZIP, per-person compute state, warnings, queue and delivery log | `/payruns*` (create, preview, employees, compute, validate, generate-pdfs, mark-paid, send, void, export.csv, zip) |
| Payslips | Register with filters (status, payrun, missing bank), detail with lines/inputs/arrears/history/downloads/PDF, bulk email by selection or period | `/payslips*`, `/payslips/send`, `/payslips/:id/{compute,lines,inputs,arrear,pdf,preview-pdf,print,history,downloads}` |
| Portal (employee) | My profile, my attendance, my time off + request form, my payslips + download through the ownership-checked route | `/portal/*`, `/auth/token-for-payslip/:id` |
| Settings | Company (≈35 switches in one documented table, format-checked), PT slabs (read), Users (create with a set-password link, one role, invite/resend, open-link history, activate/deactivate, reset password, peer-admin rules), access matrix, queues/audit with retry + reclaim | `/company`, `/salary/pt-slabs`, `/users*`, `/system/jobs`, `/system/tasks`, `/system/tasks/:id/retry`, `/system/reclaim`, `/system/audit` |
| Dashboards | Payroll run view, HR headcount view and an employee's own month, chosen by a registry and by permissions; a refusal notice for an unregistered key |
| Picklists | Indian states + PT states + leave categories from `GET /api/meta`, cached per session, with a stale stored value kept visible and labelled |
| Cross-cutting | Toasts, error panels with retry, empty states that name the fix, 12 formatters, two themes with a checked contrast ramp, scroll-aware modals, ErrorBoundary, mobile nav | — |

**Route count: 160 registered endpoints** (`node backend/scripts/routes-list.js`).

---

## 3 · On the server, deliberately not in the UI

These work if you call them (curl, a script, an integration) — no screen offers them, and that is the
honest state rather than an oversight to hide:

* `POST /api/employees/import` and `POST /api/attendance/import` — CSV ingestion with per-row errors.
  No importer screen, although the API already answers `dry_run: true` with per-row errors — what is
  missing is the three-part form (paste → preview → commit), not the backend. `GET /attendance/export.csv`
  is a button on the Attendance header now, which is the round trip for preparing a file.
* `POST /api/salary/pt-slabs`, `POST|DELETE /api/company/pt-slabs*` — Professional Tax slabs are
  **read-only** in the UI (`Company → Payroll conventions` shows the flat amount, the annual cap and the
  charge slice). Editing a slab table by hand while a month is half-computed is how a state's PT gets
  applied twice; if you need it, the modal to add is small but the audit story is not.
* `GET /api/reports/{salary-register.csv,payroll-summary.csv,attendance.csv,payslips.zip}` — a
  convenience path family kept for links and scripts. The screens use the resource-scoped equivalents
  (`/payruns/:id/export.csv`, `/payruns/zip/:id`, `/attendance/export.csv`) because those carry the
  run's state with them.
* `GET /api/employees/me`, `GET /api/contracts/employee/:id` — the portal and the employee detail page
  use `/portal/*` and `/employees/:id/contracts` instead, which apply the ownership check.
* `GET /api/meta` — a liveness stub for probes; no UI needed.

## 4 · Not built, and what stands in place of it

| Not built | What this app does instead | Smallest step if you need it |
|---|---|---|
| Real bank disbursement files (NACH/EBank credit formats, IDBI/AADHP payroll upload) | `GET /payruns/:id/export.csv` is a plain salary register: employee code, bank account, IFSC, gross/deductions/net | One generator per format under `lib/exports/`, reusing the CSV's SQL; the run needs a "disbursement batch" state to mark what was handed over |
| Cheque / cash disbursement, payment references | `mark-paid` stamps the run with who and when; the note field is free text | a `payments` table (mode, reference, amount) + one form per payslip |
| Full & final settlement, notice pay, gratuity lump sums | Contracts can be terminated with a date and the final month computes pro-rata; gratuity is a salary rule if you model it as one | a settlement service that reads the contract history and produces a slip outside a normal run |
| Retro-active revision workflows (back-dated increments with arrears across months) | `POST /payslips/:id/arrear` raises one arrear slip against a chosen run | a "revision" entity that generates arrears across a range of periods in one go |
| Loan / advance schedules with EMI recovery | `advance_percentage` (half-month advance) + a MANUAL deduction line per slip | a recoveries table with instalment rows the engine consumes |
| Multi-company, multi-currency, cost centres | one company row (`company_settings`) with `currency` / `currency_symbol` shown on the payslip | a `company_id` on every table, and tenant scoping in the queries |
| Attendance device pulls (ZKTeco/biometric files), geo-fenced clock-in | `POST /attendance/import` (CSV), `POST /attendance/clock`, a `source` column per row | a device-file parser into the same import path; geofencing needs the mobile client |
| Inbound e-mail (bounce handling, payslip requests by mail) | `email_deliveries` records QUEUED/SENT/FAILED, and a failed row is reported in the run's delivery log | a webhook into `POST /system/…` that flips the ledger row |
| Digital signatures / tamper-evident PDFs | every PDF is hashed (SHA-256 stored per document version, shown on the slip page and in the download audit) | sign with an org certificate at render time; the hash is already the input |
| Self-service onboarding | **the invitation half is built**: `POST /users/:id/invite` mails a single-use link from the request (one message per account, never a batch mail), and the transport itself is configured in the product (§1d), `/set-password?token=` sets the first password, and the account cannot sign in until it is used. Not built: a document upload step, an offer-letter PDF, and a candidate-initiated "I need an account" request | `FEATURE_INVITE` switches the whole thing off and the Users page falls back to a typed temporary password; `must_change_pw` still caps such a session at 10 minutes |
| SSO / OIDC | password login with a 5-attempt guard and a 15-minute lock-out window | `POST /auth/sso/callback` behind `FEATURE_SSO`; `user_role_grants` already supports external identities |
| A payroll "wizard" that walks the whole month | each step is a button on the run page, and the numbers are visible at every step | nothing missing structurally; it is a UX preference |
| Company GSTIN / CIN / PAN fields | `company_settings` has no such columns, so the Company screen cannot show them honestly | migration `0xx_company_tax_ids.sql` + 3 rows in the `SETTINGS` table + `gstin`/`cin`/`pan` patterns (already in `PATTERNS`) |
| Retention purge | `document_retention_years` is stored, edited and shown; nothing deletes | a nightly worker job that ages out `payslip_documents` and mail `.eml` files |

## 5 · Verified how

`bash scripts/check-syntax.sh`, `node backend/scripts/check-api-imports.js` (static import graph + real
ESM load of every backend module), `node backend/scripts/routes-list.js` (163 — 160 two rounds ago, plus the mail pair and the schedule's one-field switch),
`node backend/scripts/test-unit.js` (58 tests: the payroll engine, period math, the route contracts the new buttons need, the one-role and category validators, the refused-delete sentence, and RBAC — no database),
`node --test backend/test`, `npm run build` for the SPA, `python3 scripts/check-contrast.py` (71 files, 1138 class combinations resolved against both themes and measured with the WCAG formula — 0 under the floor; the shared `Notice` block was recoloured by its first run, since brand-100 on the light theme measured 1.00), and `npm run test:ui` (`scripts/ui-probe/`) — 35 cases that render
the screens a round touched in jsdom against a stubbed API and assert the text on them. Two real bugs came out of
writing it the first time: a row action whose `show(row)` predicate ran with no row at all, and an early
`return <NoAccess/>` sitting above two hooks in `CrudPage`, which made React render a different number of hooks
the moment `/auth/me` arrived. Round 8's six new cases caught two more while being written: `guard()` in
`utils/form.js` had its blank test inverted, and a structure row promised "click a row to open it" with no row
handler behind it. Both are asserted now, along with the payload key an API actually accepts (`remark`, not
`refuse_reason`). What `npm run build` does *not* catch — worth knowing, because it passed over both — is an undefined
identifier inside JSX or a component that renders the wrong thing, which is why the probe exists. What cannot be verified in this sandbox: anything that needs
Postgres or Redis — the seeder, `npm run smoke`, and a real SMTP delivery — so those are described by
their code path, not by an executed run.
