# Copy to .env only if you need to change something — every default works out of the box.
# Where the dev server proxies /api. In docker-compose this is http://api:4000.
VITE_PROXY_TARGET=http://localhost:4000
WEB_PORT=5173
