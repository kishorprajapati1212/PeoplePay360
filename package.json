{
  "name": "peoplepay360",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PeoplePay360 \u2014 HR & Payroll suite (PERN: PostgreSQL, Express, React, Node) with a Redis worker for bulk email and PDF generation",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "node scripts/dev.js",
    "start": "node scripts/dev.js",
    "db:migrate": "npm --prefix backend run db:migrate",
    "db:seed": "npm --prefix backend run db:seed",
    "db:reset": "npm --prefix backend run db:reset",
    "test": "npm --prefix backend run test",
    "test:unit": "node backend/scripts/test-unit.js",
    "smoke": "npm --prefix backend run smoke",
    "routes": "npm --prefix backend run routes",
    "check": "bash scripts/check-syntax.sh && npm --prefix backend run check && node backend/scripts/test-unit.js && node backend/scripts/routes-list.js | tail -1",
    "build": "npm --prefix frontend run build",
    "up": "docker compose up -d --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f api worker web"
  }
}
